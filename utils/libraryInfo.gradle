ext {
    bintrayRepo = 'maven'//name of Repo on https://bintray.com/nickunuchek
    bintrayName = 'BaseUtils'//name of project on https://bintray.com/nickunuchek/maven

    publishedGroupId = 'com.github.kolyall'//only this
    artifact = 'utils'//should be the same as module name
    artifactLocal = 'utils'//should be the same as module name
    libraryVersion = appVersionName
    group = publishedGroupId
    version = appVersionName

    libraryName = 'utils'

    libraryDescription = 'A wrapper for utils on Android'

    siteUrl = 'https://github.com/Kolyall/BaseUtils'
    gitUrl = 'https://github.com/Kolyall/BaseUtils.git'

    developerId = 'nickunuchek'
    developerName = 'Nick Unuchek'
    developerEmail = 'kolyall@tut.by'

    licenseName = 'The Apache Software License, Version 2.0'
    licenseUrl = 'http://www.apache.org/licenses/LICENSE-2.0.txt'
    allLicenses = ["Apache-2.0"]
}